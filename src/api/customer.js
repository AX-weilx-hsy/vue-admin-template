import request from '@/utils/request'

const CUSTOMER_URL = '/database/customer'

/* export function add(customerDTO) {
    return request({
        url: CUSTOMER_URL,
        method: 'post',
        data: customerDTO
    })
} */